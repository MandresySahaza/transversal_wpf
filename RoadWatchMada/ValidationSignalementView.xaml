<UserControl x:Class="RoadWatchMada.ValidationSignalementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000">

    <UserControl.Resources>
        <Style x:Key="TitreStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{StaticResource SecondaryColor}"/>
            <Setter Property="Margin" Value="20,10,20,20"/>
        </Style>

        <Style x:Key="BoutonActionStyle" TargetType="Button">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <Style x:Key="FiltreLabelStyle" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,5,0"/>
            <Setter Property="FontWeight" Value="Medium"/>
        </Style>

        <Style x:Key="FiltreTextBoxStyle" TargetType="TextBox">
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="MinWidth" Value="100"/>
        </Style>

        <Style x:Key="FiltreComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="MinWidth" Value="150"/>
        </Style>

        <Style x:Key="FiltreDatePickerStyle" TargetType="DatePicker">
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Width" Value="150"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <StackPanel>
            <!-- Titre de l'écran -->
            <TextBlock Text="Validation des Signalements" Style="{StaticResource TitreStyle}"/>

            <!-- Formulaire de filtres -->
            <Border Background="#FFF8F8F8" 
                    BorderBrush="#FFE0E0E0" 
                    BorderThickness="0,0,0,1"
                    Padding="20,15"
                    Margin="20,0,20,20">
                <StackPanel>
                    <TextBlock Text="Filtres de recherche" 
                              FontWeight="Bold" 
                              Margin="0,0,0,10"
                              Foreground="{StaticResource SecondaryColor}"/>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock Text="ID:" Style="{StaticResource FiltreLabelStyle}" Width="40"/>
                        <TextBox x:Name="txtFiltreId" 
                                Style="{StaticResource FiltreTextBoxStyle}"
                                Width="80"/>

                        <TextBlock Text="Date:" Style="{StaticResource FiltreLabelStyle}" Width="60"/>
                        <DatePicker x:Name="dpFiltreDate" 
                                   Style="{StaticResource FiltreDatePickerStyle}"/>

                        <TextBlock Text="Statut:" Style="{StaticResource FiltreLabelStyle}" Width="60"/>
                        <ComboBox x:Name="cmbFiltreStatut" 
                                 Style="{StaticResource FiltreComboBoxStyle}">
                            <ComboBoxItem Content="Tous statuts" IsSelected="True"/>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal">
                        <Button Content="Réinitialiser" 
                               Style="{StaticResource BoutonActionStyle}"
                               Background="#FF9E9E9E"
                               Foreground="White"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Tableau des signalements -->
            <DataGrid x:Name="dgSignalements" 
                     AutoGenerateColumns="False"
                     Margin="20,0,20,20"
                     IsReadOnly="True"
                     HeadersVisibility="Column"
                     SelectionMode="Single"
                     CanUserAddRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                    <DataGridTextColumn Header="Signaleur" Width="200">
                        <DataGridTextColumn.Binding>
                            <MultiBinding StringFormat="{}{0} {1}">
                                <Binding Path="NomSignaleur"/>
                                <Binding Path="PrenomSignaleur"/>
                            </MultiBinding>
                        </DataGridTextColumn.Binding>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Date Signalement" Binding="{Binding DateSignalement, StringFormat=d}" Width="120"/>
                    <DataGridTextColumn Header="Statut" Binding="{Binding Statut}" Width="120"/>
                    <!--<DataGridTextColumn Header="Catégorie" Binding="{Binding Categorie}" Width="150"/>
                    <DataGridTextColumn Header="Localisation" Binding="{Binding Localisation}" Width="200"/>-->

                    <DataGridTemplateColumn Header="Actions" Width="350">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Valider" 
                                           Style="{StaticResource BoutonActionStyle}"
                                           Background="#FF4CAF50"
                                           Foreground="White"/>
                                    <Button Content="Refuser" 
                                           Style="{StaticResource BoutonActionStyle}"
                                           Background="#FFF44336"
                                           Foreground="White"/>
                                    <Button Content="Détails" 
                                           Style="{StaticResource BoutonActionStyle}"
                                           Background="#FF2196F3"
                                           Foreground="White"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Pagination -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
                <Button Content="◀◀ Premier" 
                       Style="{StaticResource BoutonActionStyle}"/>
                <Button Content="◀ Précédent" 
                       Style="{StaticResource BoutonActionStyle}"/>

                <TextBlock Text="Page" VerticalAlignment="Center" Margin="10,0"/>
                <TextBox x:Name="txtPageCourante" 
                        Width="50" 
                        Text="1" 
                        TextAlignment="Center"
                        Margin="5,0"/>
                <TextBlock Text="/" VerticalAlignment="Center" Margin="5,0"/>
                <TextBlock x:Name="txtTotalPages" 
                          Text="1" 
                          VerticalAlignment="Center"
                          Margin="0,0,10,0"/>

                <TextBlock x:Name="txtTotalSignalements" 
                          Text="(0 signalements)" 
                          VerticalAlignment="Center"
                          Margin="20,0"
                          FontStyle="Italic"
                          Foreground="Gray"/>

                <Button Content="Suivant ▶" 
                       Style="{StaticResource BoutonActionStyle}"/>
                <Button Content="Dernier ▶▶" 
                       Style="{StaticResource BoutonActionStyle}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>